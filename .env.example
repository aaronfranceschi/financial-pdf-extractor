# Copy this file to `.env.local` and set real values. Do not commit secrets.

# Required for server-side OpenAI structuring (see app/api/financial-extract/route.ts)
OPENAI_API_KEY=

# Optional — defaults to gpt-4o
# OPENAI_MODEL=gpt-4o-mini

# Demo samples: bundled PDFs in public/samples/ (no Drive). Set true only if you want Drive links for preview / legacy proxy (fragile).
# NEXT_PUBLIC_USE_GOOGLE_DRIVE_SAMPLES=true

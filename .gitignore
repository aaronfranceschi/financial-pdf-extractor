# Dependencies
/node_modules

# Next.js
/.next/
/out/

# Env — never commit secrets (copy .env.example → .env.local and fill values locally)
.env
.env.*
!.env.example

# Debug / OS
.DS_Store
*.log
npm-debug.log*
